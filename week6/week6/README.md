# Week 6 — React JS Hands-On Labs (HOL 11–17)

Continuation of week5. Same pattern: each folder is an independent CRA
project.

```
cd HOLx-appname
npm install
npm start
```

| # | Folder | Topic |
|---|--------|-------|
| 11 | HOL11-eventexamplesapp | Events, handlers, `this` binding, synthetic events |
| 12 | HOL12-ticketbookingapp | Conditional rendering (guest vs logged-in view) |
| 13 | HOL13-bloggerapp | if/else, ternary, `&&`, element variables |
| 14 | HOL14-employee-context-app | React Context API (theme, replaces props drilling) |
| 15 | HOL15-ticketraisingapp | Controlled forms, textbox + textarea, submit handling |
| 16 | HOL16-mailregisterapp | Form validation on submit (name/email/password rules) |
| 17 | HOL17-fetchuserapp | Consuming a REST API in `componentDidMount` |

## Note on source fidelity
For HOL 11–17 the embedded code screenshots in the Word docs didn't render
in my viewer this session, so these were built directly from the detailed
written task descriptions in each doc (which fully specify component names,
props, and behavior). If anything doesn't match your original screenshots
exactly, send them over again and I'll reconcile.

## Git labs + remaining course items
Still pending: Git-HOL 1–5 and Angular HOL. See the top-level `week7/`
folder for the Angular Student Course Portal (Hands-On 1–3 fully built,
4–10 roadmapped in `ROADMAP.md`).
